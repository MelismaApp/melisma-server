{
  "name": "melisma-server",
  "version": "0.1.0",
  "private": true,
  "description": "Personal caching and merging lyrics server for Melisma",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/main.ts",
    "dev": "node --watch src/main.ts",
    "test": "node --test 'test/**/*.test.ts'",
    "key": "node scripts/key.ts"
  },
  "dependencies": {},
  "devDependencies": {}
}
